//This would be upadted when bug is fixed var jsonData = pm.response.json();

pm.test("Response code is 400", function () {
    pm.response.to.have.status(400);
});

pm.test("Status is failed", function () {
    pm.expect(jsonData.status).to.eql("failed");
});

pm.test("Error is an array", function () {
    pm.expect(jsonData.error).to.be.an("array").that.is.not.empty;
});

pm.test("Error message is correct", function () {
    pm.expect(jsonData.error[0].msg).to.eql("result note is required");
});

