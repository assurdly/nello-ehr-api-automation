var jsonData = pm.response.json();

pm.test("Response status code is 400", function () {
    pm.response.to.have.status(400);
});

pm.test("Response status is failed", function () {
    pm.expect(jsonData.status).to.eql("failed");
});

pm.test("Check Status can only be 'awaiting billing', 'billed', or 'invoiced'", function () {
        pm.expect(jsonData.error).to.eql("Status can only be 'awaiting billing', 'billed', or 'invoiced'");
    });
