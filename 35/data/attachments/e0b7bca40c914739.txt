var jsonData = pm.response.json();

pm.test("Response code is 401", () => {
    pm.response.to.have.status(401);
});

pm.test("Authorization error message is correct", () => {
    pm.expect(jsonData.message).to.eql("No token, authorization denied");
});
