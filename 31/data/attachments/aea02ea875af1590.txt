var jsonData = pm.response.json();

pm.test("Response code is 400", function () {
    pm.response.to.have.status(400);
});

pm.test("Token validation status is false", function () {
    pm.expect(jsonData.valid).to.be.false;
});

pm.test("Token validation error is returned", function () {
    pm.expect(jsonData.error).to.eql("Invalid or expired token");
});
