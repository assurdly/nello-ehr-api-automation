var jsonData = pm.response.json();

pm.test("Response status code is 400 (Bad Request)", function () {
    pm.response.to.have.status(400);
});

pm.test("Errors array is present and not empty", function () {
    pm.expect(jsonData).to.have.property("errors").that.is.an("array").that.is.not.empty;
});

pm.test("Error message is correct", function () {
    pm.expect(jsonData.errors[0].msg).to.eql("Invalid patient identifier. Ensure you’re using the correct format.");
});
